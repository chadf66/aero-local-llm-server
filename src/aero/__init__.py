"""aero — a lean, Mac-native local LLM server with an OpenAI-compatible API.

Air-derived: light enough to fly on a memory-constrained MacBook Air. The whole
design point is the inverse of a fleet server — single process, localhost,
single user, one model resident at a time, inference on the Metal GPU.
"""

__version__ = "0.1.0"
